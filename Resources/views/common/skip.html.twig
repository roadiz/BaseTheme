{% set links = [
    { id: 'content', label: 'Skip to content' },
    { id: 'footer', label: 'Skip to footer' },
]  %}

{% for link in links %}
    <a class="skip-link" href="#{{ link.id }}">{{ link.label }}</a>
{% endfor %}
