{% if themeServices.navigation|length %}
<nav class="navbar navbar-default" id="nav" data-active-node="{{ node.id }}">

	{#
	 #  Home with a node
	 #}
	{% if home %}
	<a href="{{ home|url }}" class="navbar-brand nav-link" id="nav-home">{{ meta.siteName }}</a>
	{% endif %}

	{#
	 # Main navigation
	 #}
	<ul class="nav navbar-nav" id="nav-list">
		{% for cnode in themeServices.navigation %}
			{% set source = cnode.nodeSources.first %}
			{% include '@BaseTheme/modules/nav-item.html.twig' with {
				'source' : source,
				'active' : node,
				'securityContext':securityContext,
				'themeServices':themeServices
			} only %}
		{% endfor %}
	</ul>
</nav>
{% endif %}
