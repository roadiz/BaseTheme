<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    {% include 'modules/sitemap-element.xml.twig' with {'element': home} only %}

    {% for page in projects %}
    {% include 'modules/sitemap-element.xml.twig' with {'element': page} only %}
    {% endfor %}

    {% for page in pages %}
    {% include 'modules/sitemap-element.xml.twig' with {'element': page} only %}
    {% endfor %}

    {# Static pages #}
    <url>
        <loc>{{ path('archives') }}</loc>
        <priority>0.6</priority>
    </url>
</urlset>

